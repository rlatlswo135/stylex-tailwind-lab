# StyleX × Tailwind CSS 비교 실험실

블로그 작성자가 동일한 React 화면의 구현, 장단점, 빌드 결과와 런타임 동작을 직접 비교할 수 있는 로컬 Git 저장소를 만든다.

## 범위와 완료 기준

1. 동일한 데이터·DOM·디자인으로 기본 카드의 두 구현과 원문 코드를 제공한다.
2. 반응형·상태 변형, 스타일 합성, 런타임 값 시나리오를 양쪽으로 구현한다. 장점과 비용을 함께 설명한다.
3. 서론에서 빌드 시 정적 CSS 생성이라는 공통점과 JS 스타일 객체/유틸리티 클래스, 타입, 합성, 동적 값의 차이를 설명한다.
4. 약점은 고장 난 예제를 정상 구현과 혼동시키지 않고 별도 코드와 설명으로 제시한다.
5. 동일한 대량 카드 마운트·업데이트를 독립된 production 빌드에서 측정한다. 원시 결과와 환경을 JSON으로 내보내고 JS/CSS raw·gzip 크기 및 빌드 시간을 기록한다. 합성 측정은 실제 제품 전체 성능이나 보편적 우열을 증명하지 않는다.
6. 설치 시 npm latest 안정 버전을 확인하고 정확한 버전과 lockfile을 보존한다. 확인 결과: StyleX 0.19.0, Tailwind CSS 4.3.3, TypeScript 7.0.2, Vite 8.2.2, React 19.2.8 (2026-09-08).
7. 동일 요청에 사용하는 StyleX/Tailwind 전용 create-component 스킬, 공통 평가 기준, 생성 산출물 및 검증 기록을 제공한다. 모델의 사전 지식 수준을 추측하거나 실제 수행하지 않은 RAG·토큰·소요 시간을 꾸며내지 않는다. 통제된 독립 세션의 반복 실험과 현재 세션 시연을 구분한다.

## 구조

- React + TypeScript + Vite. 라우터 없이 hash 탭으로 화면 이동.
- 비교 앱은 양쪽 소스와 화면을 보여준다. 독립 benchmark 엔트리는 해당 구현만 import한다.
- 공통 reset과 데이터, 동일한 HTML 구조를 사용한다. Tailwind Preflight가 상대 화면에 영향을 주지 않게 제외한다.
- 성능 측정은 React commit까지의 동기 작업과 다음 두 animation frame까지의 경과 시간을 분리한다. 후자는 정확한 paint/GPU 시간으로 표시하지 않는다.
- npm scripts로 개발, 타입 검사, 빌드, 크기 보고서를 실행한다.
- 로컬 저장소만 생성한다. GitHub 게시·배포는 범위에 포함하지 않는다.

## 블로그의 질문

단순 마크업 작성 편의성, 조합 규칙, 디자인 토큰, 런타임 값과 추출 제약, 에이전트에게 명시할 규칙의 차이가 어떤 코드로 나타나는가? 번들 크기나 합성 벤치마크의 차이는 그 코드와 어떤 관계가 있는가?
